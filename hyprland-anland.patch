diff --git a/src/Compositor.cpp b/src/Compositor.cpp
--- a/src/Compositor.cpp
+++ b/src/Compositor.cpp
@@ -170,6 +170,13 @@ CCompositor::CCompositor(bool onlyConfig) : m_onlyConfigVerification(onlyConfig)
 CCompositor::~CCompositor() {
     if (!m_isShuttingDown && !m_onlyConfigVerification)
         cleanup();
+
+    // ---- Anland: 清理环境 ----
+    if (getenv("ANLAND")) {
+        unsetenv("ANLAND");
+        unsetenv("EGL_PLATFORM");
+        // 不清理 ANLAND_SOCKET，可能被其他进程使用
+    }
 }
 
 void CCompositor::setRandomSplash() {
@@ -416,6 +423,10 @@ void CCompositor::initServer(std::string socketName, int socketFd) {
         throwError("CBackend::create() failed!");
     }
 
-    // TODO: headless only
+    // ---- Anland: 检测虚拟显示 ----
+    if (getenv("ANLAND")) {
+        Log::logger->log(Log::DEBUG,
+            "ANLAND: Running in virtual display mode");
+    }
 
     initAllSignals();
@@ -552,5 +565,11 @@ void CCompositor::stopCompositor() {
 void CCompositor::cleanup() {
     if (!m_wlDisplay)
         return;
 
+    // ---- Anland: 清理前通知 ----
+    if (getenv("ANLAND")) {
+        Log::logger->log(Log::DEBUG,
+            "ANLAND: Cleaning up virtual display");
+    }
+
     if (m_watchdogWriteFd.isValid()) [[maybe_unused]]
diff --git a/src/main.cpp b/src/main.cpp
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -37,6 +37,7 @@ static void help() {
     --i-am-really-stupid         - Omits root user privileges check (why would you do that?)
     --verify-config              - Do not run Hyprland, only print if the config has any errors
     --version           -v       - Print this binary's version
-    --version-json               - Print this binary's version as json)#");
+    --version-json               - Print this binary's version as json)
+    --anland                     - Use the Anland virtual display backend (Android display) )#");
 }
 
@@ -55,6 +56,9 @@ static void reapZombieChildrenAutomatically() {
 
 int main(int argc, char** argv) {
 
+    // ---- Anland virtual display backend ----
+    bool wantAnland = false;
+
     if (!getenv("XDG_RUNTIME_DIR"))
         throwError("XDG_RUNTIME_DIR is not set!");
 
@@ -174,6 +178,8 @@ int main(int argc, char** argv) {
             } else if (value == "--safe-mode") {
                 safeMode = true;
                 continue;
+            } else if (value == "--anland") {
+                wantAnland = true;
             } else if (value == "--watchdog-fd") {
                 if (std::next(it) == args.end()) {
                     help();
@@ -184,6 +190,15 @@ int main(int argc, char** argv) {
         }
     }
 
+    // ---- Anland: 设置虚拟设备环境变量 ----
+    if (wantAnland) {
+        setenv("ANLAND", "1", 1);
+        if (!getenv("ANLAND_SOCKET")) {
+            setenv("ANLAND_SOCKET", "/run/display.sock", 1);
+        }
+        setenv("EGL_PLATFORM", "surfaceless", 1);
+    }
+
     if (!ignoreSudo && NInit::isSudo()) {
         std::println(stderr,
                      "[ ERROR ] Hyprland was launched with superuser privileges, but the privileges check is not omitted.\n"
diff --git a/src/helpers/Monitor.cpp b/src/helpers/Monitor.cpp
--- a/src/helpers/Monitor.cpp
+++ b/src/helpers/Monitor.cpp
@@ -1,4 +1,6 @@
 #include "Monitor.hpp"
+#include <aquamarine/backend/anland/AnlandBackend.hpp>
+#include <aquamarine/backend/anland/AnlandOutput.hpp>
 #include "MiscFunctions.hpp"
 #include "../macros.hpp"
 #include "SharedDefs.hpp"
@@ -180,6 +182,41 @@ CMonitor::CMonitor(SP<Aquamarine::IOutput> output_) : m_name(output_->name), m_s
 void CMonitor::onConnect(bool noRule) {
     Event::bus()->m_events.monitor.preAdded.emit(m_self.lock());
+
+    // ---- Anland: 完全虚拟化 ----
+    if (m_output) {
+        auto backend = m_output->getBackend();
+        if (backend && backend->type() == Aquamarine::AQ_BACKEND_ANLAND) {
+            m_isVirtualDisplay = true;
+            m_backendOwnsRendering = true;
+            m_skipModeSet = true;
+            m_skipFenceSync = true;
+            Log::logger->log(Log::DEBUG,
+                "ANLAND: Virtual display detected - backend owns everything");
+
+            // 从 output state 获取实际尺寸
+            auto mode = m_output->state->state().mode.lock();
+            if (!mode) mode = m_output->state->state().customMode;
+            if (mode) {
+                m_size = mode->pixelSize;
+                m_pixelSize = m_size;
+                m_transformedSize = m_size;
+                m_scale = 1.0f;
+                m_position = {0, 0};
+                m_enabled = true;
+                m_renderingInitPassed = true;
+                m_forceFullFrames = 3;
+                // 延迟调度帧，确保 setupDefaultWS 先执行
+                g_pEventLoopManager->doLater([self = m_self]() {
+                    if (!self) return;
+                    self->m_output->scheduleFrame(Aquamarine::IOutput::AQ_SCHEDULE_NEW_MONITOR);
+                });
+                Log::logger->log(Log::DEBUG,
+                    "ANLAND: Virtual display set to %dx%d", (int)m_size.x, (int)m_size.y);
+            }
+        }
+    }
+
     CScopeGuard x = {[]() { g_pCompositor->arrangeMonitors(); }};
 
     m_zoomAnimProgress->setValueAndWarp(0.F);
     m_zoomAnimFrameCounter = 0;
@@ -1733,6 +1767,28 @@ bool CMonitor::applyMonitorRule(Config::CMonitorRule&& pMonitorRule) {
 
     Log::logger->log(Log::DEBUG, "Applying monitor rule for {}", m_name);
 
+    // ---- Anland: 完全跳过模式配置 ----
+    if (m_isVirtualDisplay) {
+        Log::logger->log(Log::DEBUG,
+            "ANLAND: Virtual display - skipping all mode config");
+        if (m_forceFullFrames <= 0) {
+            m_forceFullFrames = 3;
+            g_pEventLoopManager->doLater([self = m_self]() {
+                if (!self) return;
+                self->m_output->scheduleFrame(Aquamarine::IOutput::AQ_SCHEDULE_NEW_MONITOR);
+            });
+        }
+        if (m_size.x == 0 || m_size.y == 0) {
+            auto mode = m_output->state->state().mode.lock();
+            if (!mode) mode = m_output->state->state().customMode;
+            if (mode) m_size = mode->pixelSize;
+        }
+        m_pixelSize = m_size;
+        m_transformedSize = m_size;
+        if (m_position.x < 0 || m_position.y < 0) m_position = {0, 0};
+        return true;
+    }
+
     m_activeMonitorRule = std::move(pMonitorRule);
 
     if (m_forceSize.has_value())
@@ -3080,6 +3133,32 @@ bool CMonitor::gammaRampsInUse() {
 
 void CMonitorState::ensureBufferPresent() {
     const auto& STATE = m_owner->m_output->state->state();
+
+    // ---- Anland: 虚拟显示 - 从后端获取当前 buffer ----
+    if (m_owner && m_owner->m_isVirtualDisplay) {
+        if (!STATE.buffer) {
+            auto backend = m_owner->m_output->getBackend();
+            if (backend && backend->type() == Aquamarine::AQ_BACKEND_ANLAND) {
+                auto anlandBackend = dynamic_cast<Aquamarine::CAnlandBackend*>(backend.get());
+                if (anlandBackend) {
+                    auto output = anlandBackend->getOutput();
+                    if (output) {
+                        auto buffer = output->getCurrentBuffer();
+                        if (buffer) {
+                            m_owner->m_output->state->setBuffer(buffer);
+                            Log::logger->log(Log::DEBUG,
+                                "ANLAND: Set buffer from AnlandOutput to state");
+                            return;
+                        }
+                    }
+                }
+            }
+            Log::logger->log(Log::WARN,
+                "ANLAND: No buffer available from backend, skipping ensureBufferPresent");
+        }
+        return;
+    }
+
     if (!STATE.enabled) {
         Log::logger->log(Log::TRACE, "CMonitorState::ensureBufferPresent: Ignoring, monitor is not enabled");
         return;
@@ -3125,9 +3204,18 @@ bool CMonitorState::test()
 
 bool CMonitorState::updateSwapchain() {
+    // ---- Anland: 完全跳过 swapchain ----
+    if (m_owner && m_owner->m_isVirtualDisplay) {
+        Log::logger->log(Log::TRACE,
+            "ANLAND: Virtual display - no swapchain needed");
+        return true;
+    }
+
-    const auto& OPTIONS = m_owner->m_output->swapchain->currentOptions();
+    // 如果 swapchain 不存在，返回 true（模拟成功）
+    const auto& OPTIONS = m_owner->m_output->swapchain ?
+        m_owner->m_output->swapchain->currentOptions() : Aquamarine::SSwapchainOptions{};
     const auto& STATE   = m_owner->m_output->state->state();
     const auto& MODE    = STATE.mode ? STATE.mode : STATE.customMode;
     if (!MODE) {
         Log::logger->log(Log::WARN, "updateSwapchain: No mode?");
         return true;
     }
@@ -3138,10 +3227,13 @@ bool CMonitorState::updateSwapchain() {
     if (OPTIONS.format == m_owner->m_drmFormat && OPTIONS.scanout && OPTIONS.length == 3 && OPTIONS.size == MODE->pixelSize)
         return true;
 
-    auto options    = OPTIONS;
-    options.format  = m_owner->m_drmFormat;
-    options.scanout = true;
-    options.length  = 3;
-    options.size    = MODE->pixelSize;
-    return m_owner->m_output->swapchain->reconfigure(options);
+    if (m_owner->m_output->swapchain) {
+        auto options    = OPTIONS;
+        options.format  = m_owner->m_drmFormat;
+        options.scanout = true;
+        options.length  = 3;
+        options.size    = MODE->pixelSize;
+        return m_owner->m_output->swapchain->reconfigure(options);
+    }
+    return true;
 }
diff --git a/src/helpers/Monitor.hpp b/src/helpers/Monitor.hpp
--- a/src/helpers/Monitor.hpp
+++ b/src/helpers/Monitor.hpp
@@ -36,5 +36,11 @@ class CMonitorState {
 
     CMonitor* m_owner = nullptr;
+
+        // ---- Anland: 虚拟显示后端标记 ----
+        bool m_isVirtualDisplay = false;
+        bool m_backendOwnsRendering = true;
+
+        friend class CMonitor;
 };
 
 class CMonitor {
@@ -204,6 +211,12 @@ class CMonitor : public IMonitorQueryable, public IMonitorArrangeable {
     // for direct scanout
     PHLWINDOWREF m_lastScanout;
     bool         m_directScanoutIsActive    = false; // for cleanup logic. m_lastScanout.expired() can become true before the DS cleanup if client crashes/exits while DS is active.
     bool         m_scanoutNeedsCursorUpdate = false;
 
+        // ---- Anland: 虚拟显示后端标记 ----
+        bool m_isVirtualDisplay = false;
+        bool m_backendOwnsRendering = true;
+        bool m_skipModeSet = true;
+        bool m_skipFenceSync = true;
+
     // for special fade/blur
diff --git a/src/managers/PointerManager.cpp b/src/managers/PointerManager.cpp
--- a/src/managers/PointerManager.cpp
+++ b/src/managers/PointerManager.cpp
@@ -382,5 +382,10 @@ bool CPointerManager::attemptHardwareCursor(SP<CPointerManager::SMonitorPointerS
     auto output = state->monitor->m_output;
 
+    // ---- Anland: 虚拟显示不支持硬件光标 ----
+    if (state->monitor && state->monitor->m_isVirtualDisplay) {
+        return false;
+    }
+
     if (!(output->getBackend()->capabilities() & Aquamarine::IBackendImplementation::eBackendCapabilities::AQ_BACKEND_CAPABILITY_POINTER))
         return false;
 
@@ -440,5 +445,10 @@ SP<Aquamarine::IBuffer> CPointerManager::renderHWCursorBuffer(SP<CPointerManager
     auto        maxSize    = state->monitor->m_output->cursorPlaneSize();
     auto const& cursorSize = m_currentCursorImage.size;
 
+    // ---- Anland: 虚拟显示不支持硬件光标 ----
+    if (state->monitor && state->monitor->m_isVirtualDisplay) {
+        return nullptr;
+    }
+
     static auto PCPUBUFFER = CConfigValue<Config::INTEGER>("cursor:use_cpu_buffer");
 
diff --git a/src/render/GLRenderer.cpp b/src/render/GLRenderer.cpp
--- a/src/render/GLRenderer.cpp
+++ b/src/render/GLRenderer.cpp
@@ -1,6 +1,7 @@
 #include "GLRenderer.hpp"
 #include "decorations/CHyprInnerGlowDecoration.hpp"
 #include <aquamarine/output/Output.hpp>
+#include <cstring>
 #include "../config/ConfigValue.hpp"
 #include "../managers/CursorManager.hpp"
 #include "../managers/PointerManager.hpp"
@@ -45,7 +46,14 @@ bool CHyprGLRenderer::initRenderBuffer(SP<Aquamarine::IBuffer> buffer, uint32_t
         Log::logger->log(Log::ERR, "getOrCreateRenderbuffer failed for {}", NFormatUtils::drmFormatName(fmt));
         return false;
     }
 
+    // ---- Anland: 虚拟显示完全跳过 renderbuffer ----
+    if (m_renderData.pMonitor && m_renderData.pMonitor->m_isVirtualDisplay) {
+        Log::logger->log(Log::TRACE,
+            "ANLAND: Virtual display - backend manages renderbuffer");
+        return true;
+    }
+
     return m_currentRenderbuffer;
 }
 
@@ -102,6 +110,20 @@ bool CHyprGLRenderer::beginRenderInternal(PHLMONITOR pMonitor, CRegion& damage,
 
 void CHyprGLRenderer::endRender(const std::function<void()>& renderingDoneCallback) {
     const auto  PMONITOR           = g_pHyprRenderer->m_renderData.pMonitor;
+
+    // ---- Anland: 虚拟显示完全跳过 explicit sync 和 GLFB 操作 ----
+    if (PMONITOR && PMONITOR->m_isVirtualDisplay) {
+        Log::logger->log(Log::TRACE,
+            "ANLAND: Virtual display - backend manages sync, skipping GLFB operations");
+        m_usedAsyncBuffers.clear();
+        g_pHyprRenderer->m_renderData.damage = {};
+        g_pHyprRenderer->m_renderData.pMonitor.reset();
+        if (renderingDoneCallback) {
+            renderingDoneCallback();
+        }
+        return;
+    }
+
     static auto PNVIDIAANTIFLICKER = CConfigValue<Config::INTEGER>("opengl:nvidia_anti_flicker");
 
     g_pHyprRenderer->m_renderData.damage = m_renderPass.render(g_pHyprRenderer->m_renderData.damage);
diff --git a/src/render/OpenGL.cpp b/src/render/OpenGL.cpp
--- a/src/render/OpenGL.cpp
+++ b/src/render/OpenGL.cpp
@@ -11,6 +11,7 @@
 #include "OpenGL.hpp"
 #include "Renderer.hpp"
 #include "../Compositor.hpp"
+#include "../helpers/env/Env.hpp"
 #include "../helpers/MiscFunctions.hpp"
 #include "../helpers/CursorShapes.hpp"
 #include "../helpers/TransferFunction.hpp"
@@ -100,6 +101,24 @@ static int openRenderNode(int drmFd) {
     return renderFD;
 }
 
+// ---- Anland: 虚拟显示检测 ----
+static bool isVirtualDisplay() {
+    const char* anland = getenv("ANLAND");
+    if (anland && strcmp(anland, "1") == 0)
+        return true;
+    const char* eglPlatform = getenv("EGL_PLATFORM");
+    if (eglPlatform && strcmp(eglPlatform, "surfaceless") == 0)
+        return true;
+    return false;
+}
+
+static bool hasSurfacelessEGL() {
+    const char* extensions = eglQueryString(EGL_NO_DISPLAY, EGL_EXTENSIONS);
+    if (!extensions) return false;
+    return strstr(extensions, "EGL_MESA_platform_surfaceless") != nullptr ||
+           strstr(extensions, "EGL_EXT_platform_device") != nullptr;
+}
+
 static ShaderFeatureFlags globalFeatures() {
     return g_pHyprRenderer->m_renderData.pMonitor && g_pHyprRenderer->m_renderData.pMonitor->needsUnmodifiedCopy() && g_pHyprRenderer->m_renderData.currentFB->getMirrorTexture() ?
         SH_FEAT_MIRROR :
@@ -110,7 +129,23 @@ void CHyprOpenGLImpl::initEGL(bool gbm) {
 
     attrs.push_back(EGL_NONE);
 
-    m_eglDisplay = m_proc.eglGetPlatformDisplayEXT(gbm ? EGL_PLATFORM_GBM_KHR : EGL_PLATFORM_DEVICE_EXT, gbm ? m_gbmDevice : m_eglDevice, attrs.data());
+    // ---- Anland: 使用 surfaceless EGL ----
+    if (isVirtualDisplay() && hasSurfacelessEGL()) {
+        Log::logger->log(Log::DEBUG,
+            "ANLAND: Using EGL_MESA_platform_surfaceless");
+        m_eglDisplay = m_proc.eglGetPlatformDisplayEXT(
+            EGL_PLATFORM_SURFACELESS_MESA, EGL_DEFAULT_DISPLAY, attrs.data());
+        if (m_eglDisplay == EGL_NO_DISPLAY) {
+            Log::logger->log(Log::DEBUG,
+                "ANLAND: Falling back to EGL_EXT_platform_device");
+            m_eglDisplay = m_proc.eglGetPlatformDisplayEXT(
+                EGL_PLATFORM_DEVICE_EXT, m_eglDevice, attrs.data());
+        }
+    } else {
+        m_eglDisplay = m_proc.eglGetPlatformDisplayEXT(
+            gbm ? EGL_PLATFORM_GBM_KHR : EGL_PLATFORM_DEVICE_EXT,
+            gbm ? m_gbmDevice : m_eglDevice, attrs.data());
+    }
     if (m_eglDisplay == EGL_NO_DISPLAY)
         RASSERT(false, "EGL: failed to create a platform display");
 
@@ -267,14 +302,29 @@ CHyprOpenGLImpl::CHyprOpenGLImpl() : m_drmFD(g_pCompositor->m_drmRenderNode.fd
 
     RASSERT(eglBindAPI(EGL_OPENGL_ES_API) != EGL_FALSE, "Couldn't bind to EGL's opengl ES API. This means your gpu driver f'd up. This is not a hyprland issue.");
 
+    // ---- Anland: 虚拟显示直接使用 surfaceless ----
+    bool useSurfaceless = isVirtualDisplay() && hasSurfacelessEGL();
+
     bool success = false;
-    if (EGLEXTENSIONS.contains("EXT_platform_device") || !m_proc.eglQueryDevicesEXT || !m_proc.eglQueryDeviceStringEXT) {
+    if (useSurfaceless) {
+        Log::logger->log(Log::DEBUG,
+            "ANLAND: Virtual display - using surfaceless EGL");
+        success = true;
+        initEGL(false);
+    } else if (EGLEXTENSIONS.contains("EXT_platform_device") || !m_proc.eglQueryDevicesEXT || !m_proc.eglQueryDeviceStringEXT) {
         m_eglDevice = eglDeviceFromDRMFD(m_drmFD);
 
         if (m_eglDevice != EGL_NO_DEVICE_EXT) {
             success = true;
             initEGL(false);
         }
+    } else {
+        success = false;
+    }
+
+    if (!success && useSurfaceless) {
+        initEGL(false);
+        success = true;
     }
 
     if (!success) {
@@ -290,6 +340,14 @@ CHyprOpenGLImpl::CHyprOpenGLImpl() : m_drmFD(g_pCompositor->m_drmRenderNode.fd
         }
     }
 
+    // ---- Anland: 最后尝试 surfaceless ----
+    if (!success && !useSurfaceless && hasSurfacelessEGL()) {
+        Log::logger->log(Log::WARN,
+            "ANLAND: Falling back to surfaceless EGL");
+        initEGL(false);
+        success = true;
+    }
+
     RASSERT(success, "EGL does not support KHR_platform_gbm or EXT_platform_device, this is an issue with your gpu driver.");
 
     auto* const EXTENSIONS = rc<const char*>(glGetString(GL_EXTENSIONS));
diff --git a/src/render/Renderer.cpp b/src/render/Renderer.cpp
--- a/src/render/Renderer.cpp
+++ b/src/render/Renderer.cpp
@@ -1,5 +1,7 @@
 #include "Renderer.hpp"
 #include "../Compositor.hpp"
+#include <aquamarine/backend/anland/AnlandBackend.hpp>
+#include <aquamarine/backend/anland/AnlandOutput.hpp>
 #include "../helpers/math/Math.hpp"
 #include <algorithm>
 #include <aquamarine/output/Output.hpp>
@@ -483,34 +485,102 @@
         return beginFullFakeRenderInternal(pMonitor, damage, fb, simple);
 
     int bufferAge = 0;
 
     if (!buffer) {
-        m_currentBuffer = pMonitor->m_output->swapchain->next(&bufferAge);
-        if (!m_currentBuffer) {
-            Log::logger->log(Log::ERR, "Failed to acquire swapchain buffer for {}", pMonitor->m_name);
-            return false;
-        }
-    } else
+        // ---- Anland: 虚拟显示 - 从后端获取当前 buffer ----
+        if (pMonitor->m_isVirtualDisplay) {
+            // 确保GL上下文当前
+            if (g_pHyprOpenGL) {
+                g_pHyprOpenGL->makeEGLCurrent();
+            }
+            auto backend = pMonitor->m_output->getBackend();
+            if (backend && backend->type() == Aquamarine::AQ_BACKEND_ANLAND) {
+                auto anlandBackend = dynamic_cast<Aquamarine::CAnlandBackend*>(backend.get());
+                if (anlandBackend) {
+                    auto output = anlandBackend->getOutput();
+                    if (output) {
+                        m_currentBuffer = output->getCurrentBuffer();
+                        if (m_currentBuffer) {
+                            pMonitor->m_output->state->setBuffer(m_currentBuffer);
+                            bufferAge = 0;
+                        }
+                    }
+                }
+            }
+            if (!m_currentBuffer) {
+                Log::logger->log(Log::ERR, "ANLAND: Failed to acquire buffer for virtual display");
+                return false;
+            }
+        } else {
+            m_currentBuffer = pMonitor->m_output->swapchain->next(&bufferAge);
+            if (!m_currentBuffer) {
+                Log::logger->log(Log::ERR, "Failed to acquire swapchain buffer for {}", pMonitor->m_name);
+                return false;
+            }
+        }
+    } else {
         m_currentBuffer = buffer;
+    }
 
-    initRender();
+    // ---- Anland: 虚拟显示跳过 renderbuffer 初始化 ----
+    if (!pMonitor->m_isVirtualDisplay) {
+        initRender();
 
-    if (!initRenderBuffer(m_currentBuffer, pMonitor->m_output->state->state().drmFormat)) {
-        Log::logger->log(Log::ERR, "failed to start a render pass for output {}, no RBO could be obtained", pMonitor->m_name);
-        return false;
-    }
+        if (!initRenderBuffer(m_currentBuffer, pMonitor->m_output->state->state().drmFormat)) {
+            Log::logger->log(Log::ERR, "failed to start a render pass for output {}, no RBO could be obtained", pMonitor->m_name);
+            return false;
+        }
 
-    if (m_renderMode == RENDER_MODE_NORMAL) {
-        damage = pMonitor->m_damage.getBufferDamage(bufferAge);
-        pMonitor->m_damage.rotate();
-    }
+        if (m_renderMode == RENDER_MODE_NORMAL) {
+            damage = pMonitor->m_damage.getBufferDamage(bufferAge);
+            pMonitor->m_damage.rotate();
+        }
 
-    const auto  res     = beginRenderInternal(pMonitor, damage, simple);
-    static bool initial = true;
-    if (initial) {
-        initAssets();
-        initial = false;
+        const auto  res     = beginRenderInternal(pMonitor, damage, simple);
+        static bool initial = true;
+        if (initial) {
+            initAssets();
+            initial = false;
+        }
+
+        return res;
+    } else {
+        // ---- Anland: 虚拟显示 - 使用 AnlandOutput 的 FBO ----
+        auto backend = pMonitor->m_output->getBackend();
+        if (backend && backend->type() == Aquamarine::AQ_BACKEND_ANLAND) {
+            auto anlandBackend = dynamic_cast<Aquamarine::CAnlandBackend*>(backend.get());
+            if (anlandBackend) {
+                auto output = anlandBackend->getOutput();
+                if (output) {
+                    if (!output->beginRender()) {
+                        Log::logger->log(Log::ERR, "ANLAND: output->beginRender() failed");
+                        return false;
+                    }
+
+                    GLuint fbo = output->getCurrentFramebuffer();
+                    if (fbo != 0) {
+                        glBindFramebuffer(GL_FRAMEBUFFER, fbo);
+                        setViewport(0, 0, pMonitor->m_pixelSize.x, pMonitor->m_pixelSize.y);
+
+                        auto dummyFB = makeShared<AnlandDummyFB>();
+                        dummyFB->alloc(pMonitor->m_pixelSize.x, pMonitor->m_pixelSize.y, DRM_FORMAT_ABGR8888);
+
+                        bindFB(dummyFB);
+                        m_renderData.mainFB = dummyFB;
+                        m_renderData.outFB = dummyFB;
+                        m_renderData.currentFB = dummyFB;
+                    } else {
+                        Log::logger->log(Log::ERR, "ANLAND: getCurrentFramebuffer returned 0");
+                        return false;
+                    }
+                }
+            }
+        }
+
+        if (m_renderMode == RENDER_MODE_NORMAL) {
+            damage = pMonitor->m_damage.getBufferDamage(0);
+            pMonitor->m_damage.rotate();
+        }
+        return true;
     }
-
-    return res;
 }
@@ -537,6 +583,13 @@ void IHyprRenderer::renderMonitor(PHLMONITOR pMonitor, bool commit) {
 
     Event::bus()->m_events.render.pre.emit(pMonitor);
 
+    // ---- Anland: 虚拟显示强制渲染 ----
+    if (pMonitor->m_isVirtualDisplay && !pMonitor->m_output->needsFrame) {
+        if (pMonitor->m_forceFullFrames == 0) {
+            pMonitor->m_output->scheduleFrame(Aquamarine::IOutput::AQ_SCHEDULE_RENDER_MONITOR);
+        }
+    }
+
     const auto NOW = Time::steadyNow();
 
     // check the damage
diff --git a/src/render/Renderer.hpp b/src/render/Renderer.hpp
--- a/src/render/Renderer.hpp
+++ b/src/render/Renderer.hpp
@@ -4,6 +4,7 @@
 #include "../helpers/math/Math.hpp"
 #include "../helpers/time/Time.hpp"
 #include "../../protocols/cursor-shape-v1.hpp"
 #include "desktop/view/Popup.hpp"
+#include "gl/GLFramebuffer.hpp"
 #include "Framebuffer.hpp"
 #include "Texture.hpp"
@@ -25,6 +25,38 @@
 
 namespace Render {
+// ---- Anland: 虚拟显示专用占位 FBO，继承自 CGLFramebuffer 以保证 GLFB 宏安全 ----
+// 不分配任何 GL 资源，所有操作均为空或返回默认值
+class AnlandDummyFB : public GL::CGLFramebuffer {
+public:
+    AnlandDummyFB() : GL::CGLFramebuffer("anland-dummy") {
+        // 标记为已分配，避免调用方检查失败
+        m_fbAllocated = true;
+    }
+    virtual ~AnlandDummyFB() = default;
+
+    // 重写必要的虚函数（基类中为虚函数的才加 override）
+    virtual bool alloc(int w, int h, DRMFormat format = DRM_FORMAT_ARGB8888) override {
+        m_size = {w, h};
+        m_drmFormat = format;
+        return true;
+    }
+    virtual void release() override {}
+    virtual bool readPixels(CHLBufferReference buffer, uint32_t offsetX = 0, uint32_t offsetY = 0,
+                            uint32_t width = 0, uint32_t height = 0) override {
+        return false;
+    }
+    virtual void bind() override {
+        // 空，避免覆盖 Anland 已经绑定的 FBO
+    }
+    virtual void addStencil(SP<ITexture> tex) override {}
+
+protected:
+    virtual bool internalAlloc(int w, int h, DRMFormat format = DRM_FORMAT_ARGB8888) override {
+        return true;
+    }
+};
+
     using CScopeGuard = Hyprutils::Utils::CScopeGuard;
 
     class IElementRenderer;
     class CRenderPass;
